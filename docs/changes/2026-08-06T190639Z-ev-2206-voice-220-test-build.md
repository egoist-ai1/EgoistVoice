# EV-2206 and Voice 2.2.0 local test build

- UTC: `2026-08-06T19:06:39Z`
- Why: provide a working Voice candidate that interoperates with the new shared
  Translator Engine instead of the legacy fixed HTTP port.
- Changed: vendored exact `net8.0` Contracts/Client `1.0.0` DLLs with a
  hash-bound manifest; added current-user named-pipe Host discovery/start,
  typed failures, bounded target languages and fail-closed delivery.
- Coexistence: setup registers and removes only
  `%LOCALAPPDATA%\EGOIST\TranslationEngine\owners\egoist-voice.owner.json`.
  Voice never kills the shared Host or deletes the shared Engine/model tree.
- Build hardening: the release script validates manifest shape, file allowlist,
  paths, sizes and SHA-256 before test/publish; the final ISS was compiled by
  pinned Inno 6.2.1 after an isolated compatibility gate.
- Checks: Release build `0` warnings/`0` errors; `466/466` tests; changed-file
  format clean; independent outcome reviews `pass`.
- Artifact: unsigned `EgoistVoice-Setup-2.2.0-win-x64.exe`, `1,283,478,780`
  bytes, SHA-256
  `9cdea920583a7d04efa8b54bb420e4bc2941ff8c78b6a7665a53390da89cc4fe`.
- Safety: the setup was compiled but never executed on the development host;
  no tag, signing, push or publication occurred.
- Residual: the field build contains full offline ASR but uses the separate
  shared Engine/model package for translation. Clean-VM lifecycle and a single
  all-in-one full-MT bundle remain EV-2213/EV-2214 and EV-2209 work. Measured
  ASR accuracy remains unclaimed without the private corpus.
- Next: user field test, then EV-2205 residual formatting and EV-2207/EV-2208.
